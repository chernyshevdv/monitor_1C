<tr>
	<td--</td>
	<td><%= 
			if lots_difference.article.nil?
				"[Пустая номенклатура]"
			else
				"#{lots_difference.article.code} #{lots_difference.article.title}" 
			end
		%>
	</td>
	<td class="right"><%= lots_difference.hq_q %></td>
	<td class="right"><%= lots_difference.cmp_q %></td>
	<td class="right"><%= (lots_difference.hq_q || 0) - (lots_difference.cmp_q || 0) %></td>
</tr>